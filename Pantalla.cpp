#include "Pantalla.h"

pantalla::pantalla(byte t1, byte t2, byte t3)
  : indices{t1, t2, t3} {}

void pantalla::set_up() {
}

void pantalla::enviar(datos dataArray[]) {
  for (int i = 0; i < 3; i++) {
  
  }
}
